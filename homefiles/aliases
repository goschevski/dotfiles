# List files
alias ls='ls -G'
alias la='ls -lAFh'

# Back folders
alias .='cd ../'
alias ..='cd ../../'
alias ...='cd ../../../'
alias ....='cd ../../../../'

# Folders
alias sites="cd ~/Sites"
alias downloads="cd ~/Downloads"
alias desktop="cd ~/Desktop"

# Brew and cask
alias cask="brew cask"
alias cleanup="brew cleanup && brew cask cleanup"

# Trash
alias emptytrash="sudo rm -rf ~/.Trash/*"

# Clean install of node modules
alias npmi="sudo -v && rm -rf node_modules && npm cache clean && npm i"

# Git
alias st='open -a SourceTree'
function retag () {
    git tag -d $1 && git push origin :refs/tags/$1 && git tag $1
}

# Server
alias restartApache='sudo apachectl restart'
alias editApacheConf='sudo vim /etc/apache2/httpd.conf'
alias editHosts='sudo vim /etc/hosts'
alias editVhosts='sudo vim /etc/apache2/extra/httpd-vhosts.conf'

# SSH
alias vps="ssh root@188.40.242.169 -t"
